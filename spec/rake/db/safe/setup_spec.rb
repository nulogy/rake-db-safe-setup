require "spec_helper"

RSpec.describe Rake::Db::Safe::Setup do
  it "has a version number" do
    expect(Rake::Db::Safe::Setup::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
